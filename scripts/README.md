# Scripts

This folder is for one-time setup scripts and utility scripts that don't need to be in version control.

## Usage

Place any setup scripts, data migration scripts, or other one-time utilities here.

## Note

This folder is excluded from version control via `.gitignore`.
